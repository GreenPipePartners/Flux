# Sim

Flux Sim owns simulated device and tag intent.

It imports provider exports, reconstructs tag trees, selects simulated devices, and materializes enabled catalog rows into runtime FieldAgent endpoint/device/tag configuration.

See `operator-guide.md#field-simulation-operator-workflow`.
