# Local Service

Install or refresh the local service:

```bash
flux install-service
```

Start Flux:

```bash
flux start
```

Check health:

```bash
flux doctor
```

Follow logs:

```bash
flux logs
```

Stop Flux:

```bash
flux stop
```
