# Site Auditor Agent Notices

Append-only inbox for Coordinator notices to Site Auditor. Coordinator appends notices below; Site Auditor appends outcomes when notices are handled.

## Notice: 2026-05-24-coordinator-002

- Status: open
- Priority: medium
- Horizon: next-run
- Source: Coordinator
- Target agent: Site Auditor
- Feature/context: Ignition Bridges Comp Surface cleanup
- Request: Audit the Ignition Bridges summary/detail/configure UI for the reported cleanup issues: top-left Flux.links/copy widget link does not work in detail view; configure view uses `admin` as the base entry in Fluxy base URL and should not; `Token set` appears superfluous/confusing; `Clear stored token` feels unclear and needs better explanation or placement; Ignition build string like `(b2026042713)` should have a hover/help affordance if still shown.
- Acceptance signal: Site audit report or notice outcome names each issue as reproduced/not reproduced, identifies affected route/template/element when discoverable, and recommends implementation cleanup without changing application code.
- Files/context: User pasted Ignition Bridges card/detail content showing `default / Simulator`, endpoint `http://localhost:8088/system/webdev/flux`, status `Connected`, `Token set`, tested timestamp, and note `Connected to Ignition 8.3.6 (b2026042713).`; likely Comp Surface mode controls and Flux.links/copy-widget behavior.
- Notes: User said “send to cleanup”; no Cleanup agent exists in Coordinator map, so this is routed to Site Auditor as the UI/interaction owner for next-run verification and cleanup handoff evidence.
- Outcome 2026-05-24 (Site Auditor parameter update): Added this notice to next-run audit parameters in `site_audit/core_area_files.md` under Ignition Bridges. Routes/surfaces queued: `/`, `/?card=bridges&mode=detail`, and `/?card=bridges&mode=configure`; elements queued: `#bridges-comp-card`, `#bridges-comp-focus`, `[data-bridge-copy]`, Fluxy base URL field, token-state text, clear-token checkbox, and Ignition build-string text. Commands/browser checks: no live browser run; read notice context and mapped `web/Flux/src/templates/dashboard/home.html`. Findings: not reproduced yet. Blockers: needs running dashboard Command Center with bridge fixture/runtime data. Remaining follow-up: reproduce each reported cleanup issue and report affected route/template/element without changing application code.
- Outcome 2026-05-24 (Site Auditor cleanup audit): Routes/surfaces checked with browser: `/` plus real glyph clicks on `#bridges-comp-card` for detail/configure/summary in a Django StaticLiveServer fixture. Commands: `uv run python manage.py check`; `uv run pytest -q ../../site_audit/test_playwright_cleanup_audit.py`. Findings: detail copy button reproduced as working, including table-copy popover, LLM second-click popover, and docs URL `http://localhost:8001/apps/dashboard/#ignition-bridges`; configure base URL value was `http://localhost:8088/system/webdev/flux` and did not contain `admin`; token wording showed `Stored token saved`, `Remove stored token`, blank-token preservation, and write-only help; build string `(b2026042713)` had title help describing Ignition build identifier. Blockers: persistent dev server not used; fixture evidence recorded in `site_audit/latest.json`. Remaining follow-up: none for the listed bridge cleanup items unless persistent-dev data disagrees.

## Notice: 2026-05-24-coordinator-006

- Status: open
- Priority: medium
- Horizon: next-run
- Source: Coordinator
- Target agent: Site Auditor
- Feature/context: Runtime service observability UI
- Request: Audit dashboard runtime service displays for the expected cleanup: OPC server runtime rows and Flux.serve Observed Health need more operational detail, including PID and port where available; long service/detail lists need pagination; and stale/unknown runtime evidence must not be presented as simply `running`.
- Acceptance signal: Site audit report or notice outcome identifies the affected dashboard route/templates/elements, verifies whether PID/port are visible or missing, verifies pagination expectations for Flux.serve Observed Health, and records any dependency on architecture notice `2026-05-24-coordinator-005` before implementation cleanup.
- Files/context: User pasted `OPC server runtime` rows for eight endpoints all showing `running` and `■ Stop`; user also pasted `Flux.serve Observed Health` with `Healthy 17`, `Warning 3`, `Error 0`, `Stale snapshots 1`. Relevant UI appears in `web/Flux/src/templates/dashboard/home.html` sim-config and serve focuses.
- Notes: Coordinator review found PID exists in `FieldAgentHeartbeat.process_id` and FieldAgent port can be derived by architecture code, but the dashboard path may not yet expose a truth-checked metadata contract. Treat this as UI audit/cleanup evidence, not application-code permission.
- Outcome 2026-05-24 (Site Auditor parameter update): Added this notice to next-run audit parameters in `site_audit/core_area_files.md` under Runtime Service Observability. Routes/surfaces queued: `/`, `/?card=sim-config&mode=detail`, `/?card=sim-config&mode=configure`, and `/?card=serve&mode=detail`; elements queued: `#sim-config-comp-focus`, OPC server runtime rows, `#serve-comp-focus`, and Flux.serve Observed Health rows. Commands/browser checks: no live browser run; read notice context and mapped `web/Flux/src/templates/dashboard/home.html`. Findings: not reproduced yet. Blockers: needs running dashboard data and architecture metadata contract clarity from notice `2026-05-24-coordinator-005`. Remaining follow-up: verify PID/port visibility, stale/unknown labeling, and pagination/bounding for long lists.
- Outcome 2026-05-24 (Site Auditor cleanup audit): Routes/surfaces checked with browser: `/`, `sim-config` detail/configure/summary glyph swaps, and `serve` detail/summary glyph swaps in a seeded StaticLiveServer fixture. Commands: `uv run python manage.py check`; `uv run pytest -q ../../site_audit/test_playwright_cleanup_audit.py`. Findings: OPC runtime rows showed PID/port where evidence existed (`PID 12345`, `port 5061`) and stale heartbeat wording (`stored running · stale heartbeat`); Flux.serve observed-health rows showed PID/port metadata, stale snapshot summary, and unknown observed state. Remaining issue: `#serve-comp-focus .stale-list` rendered 56 observed-health rows with `pagination_controls=0`, so long-list pagination/bounding is still open. Blockers: persistent dev-server data not checked. Remaining follow-up: implement/verify Flux.serve list bounding or pagination.

## Notice: 2026-05-24-coordinator-007

- Status: open
- Priority: medium
- Horizon: next-run
- Source: Coordinator
- Target agent: Site Auditor
- Feature/context: Flux.live stale recovery and live import cleanup
- Request: Audit the Flux.live Stale Tag Recovery and live CSV import configure UI for the reported cleanup: first stale rows like Demo Area Pump/Tank reads show only `Last read older than 120s` and need clearer status/source context; rows with `Bad quality: Error_Configuration("Server "Flux Field" does not exist.")` likely represent legacy data that should be isolated or cleaned up; the `Replace existing cards for imported scopes` checkbox is visually offset from its comment/label; and `Default scope slug` should default to `Fluxolot` instead of `pad-overview`.
- Acceptance signal: Site audit report or notice outcome reproduces or rejects each visual/content issue, names the affected element/template, and records the expected cleanup behavior for future implementation without deleting legacy data until ownership is clear.
- Files/context: User pasted `Flux.live Stale Tag Recovery` with stale count `24`, first four Demo Area tags older than 120s, and multiple Meter/Tank tags reporting missing `Flux Field`; `web/Flux/src/templates/dashboard/home.html` live focus/configure panel includes stale rows, `live_scope_csv`, `Default scope slug` placeholder `pad-overview`, and `replace_live_scope` checkbox.
- Notes: User said the latter `Flux Field does not exist` rows are probably legacy cleanup. This is routed to Site Auditor because no Cleanup agent exists; if data deletion/migration is needed, future Sam/Build or Architect should decide.
- Outcome 2026-05-24 (Site Auditor parameter update): Added this notice to next-run audit parameters in `site_audit/core_area_files.md` under Flux.live Stale Recovery And CSV Import. Routes/surfaces queued: `/`, `/?card=live&mode=detail`, `/?card=live&mode=configure`, `/live/`, and `/live/pad-overview/`; elements queued: `#live-comp-focus`, stale rows, `live_scope_csv`, `live_scope`, and `replace_live_scope` controls. Commands/browser checks: no live browser run; read notice context and mapped `web/Flux/src/templates/dashboard/home.html`. Findings: not reproduced yet. Blockers: needs stale tag/runtime fixture data. Remaining follow-up: verify status/source context, legacy missing-`Flux Field` isolation, checkbox alignment, and `Fluxolot` default/suggestion without deleting legacy data.
- Outcome 2026-05-24 (Site Auditor cleanup audit): Routes/surfaces checked with browser: `/` plus `live` detail/configure/summary glyph swaps in a seeded StaticLiveServer fixture. Commands: `uv run python manage.py check`; `uv run pytest -q ../../site_audit/test_playwright_cleanup_audit.py`. Findings: stale rows include source context (`Source [default]...`); missing `Flux Field` provider rows are labeled `legacy source missing` with `Legacy cleanup candidate`; `Default scope slug` value and placeholder are `Fluxolot`; no legacy data was deleted by audit work. Remaining issue: replace-checkbox row existed but the browser alignment smoke returned false, so visual alignment still needs review in a persistent UI. Blockers: persistent dev-server data not checked. Remaining follow-up: adjust/recheck checkbox alignment.

## Notice: 2026-05-24-coordinator-009

- Status: open
- Priority: medium
- Horizon: next-run
- Source: Coordinator
- Target agent: Site Auditor
- Feature/context: Flux.charts dashboard large-list cleanup
- Request: Audit and preserve the expected Flux.charts UI behavior: remove the thousand individual chart/well `Open` links from the dashboard detail list, preserve the existing single-page navigation/stress-test path with next/forward behavior, ensure large chart lists are paginated, and add a `What is this?` affordance for `Import chart CSV` with an example layout pop-down.
- Acceptance signal: Site audit report or notice outcome verifies current large-link rendering, identifies the relevant route/template/elements, states pagination expectations for dashboard and chart views, and notes dependencies on Architect notice `2026-05-24-coordinator-008` and Docs Steward notice `2026-05-24-coordinator-010`.
- Files/context: User pasted `Flux.charts` readiness context showing `1004 charts, 8067 signals`; `web/Flux/src/templates/dashboard/home.html` trace focus renders `Navigation wells` plus an `Open` link for every enabled `TraceProfile`, and configure focus renders `Import chart CSV`.
- Notes: User explicitly warned not to delete source data while removing single-page links from the interface. Treat deletion/truncation as out of scope for Site Auditor.
- Outcome 2026-05-24 (Site Auditor parameter update): Added this notice to next-run audit parameters in `site_audit/core_area_files.md` under Flux.charts Large-List Cleanup. Routes/surfaces queued: `/`, `/?card=trace&mode=detail`, `/?card=trace&mode=configure`, `/charts/`, `/charts/wells/`, and `/charts/fluxolot/`; elements queued: `#trace-comp-focus`, chart detail `.stale-list`, per-profile `Open` links, and `Import chart CSV` controls. Commands/browser checks: no live browser run; read notice context and mapped `web/Flux/src/templates/dashboard/home.html`. Findings: not reproduced yet. Blockers: needs large TraceProfile fixture/runtime data and dependencies on Architect notice `2026-05-24-coordinator-008` plus Docs Steward notice `2026-05-24-coordinator-010`. Remaining follow-up: verify large-link rendering, pagination/bounding, preserved navigation path, and `What is this?` affordance expectations without deleting source data.
- Outcome 2026-05-24 (Site Auditor cleanup audit): Routes/surfaces checked with browser: `/` plus `trace` detail/configure/summary glyph swaps, `/charts/?card=trace-paths&mode=detail` page 1/2, and `/charts/wells/` in a seeded StaticLiveServer fixture with 64 chart profiles. Commands: `uv run python manage.py check`; `uv run pytest -q ../../site_audit/test_playwright_cleanup_audit.py`. Findings: dashboard Flux.charts detail rendered 2 aggregate `Open` links only and did not show per-profile `chart-055`; `/charts/` path list pagination worked with Next/Previous and `chart-055` only on page 2; Import chart CSV had `What is this?` and an example layout; `/charts/wells/` loaded and previous/next well controls were visible. Blockers: persistent dev-server data not checked. Remaining follow-up: none for dashboard/chart pagination cleanup, aside from separately owned docs/architecture dependencies.

## Notice: 2026-05-24-coordinator-011

- Status: open
- Priority: medium
- Horizon: next-run
- Source: Coordinator
- Target agent: Site Auditor
- Feature/context: Table copy affordance standard
- Request: Audit table-like dashboard/detail sections for the new expectation that every table has a top-right copy icon that copies the entire table contents. This includes Markdown/LLM pasted tables such as Ignition Bridges, Flux.live Stale Tag Recovery, Flux.charts readiness, and Flux.serve Observed Health.
- Acceptance signal: Site audit report or notice outcome lists representative table surfaces with copy icon present/missing, verifies placement at the table top-right, and flags where existing Flux.links/copy widgets can be reused versus where a table-level copy affordance is absent.
- Files/context: Dashboard Comp Surface details and readiness cards; existing copy widgets in `web/Flux/src/templates/dashboard/home.html`; copy-context helpers in `web/Flux/src/dashboard/copy_context.py`.
- Notes: User said “To all tables, add a copy icon at the top-right to make the entire table contents copyable.” No Cleanup agent exists, so this is recorded for Site Auditor to monitor and future cleanup to implement.
- Outcome 2026-05-24 (Site Auditor parameter update): Added this notice to next-run audit parameters in `site_audit/core_area_files.md` under Table Copy Affordance Standard. Representative surfaces queued: Ignition Bridges, OPC server runtime, Flux.live Stale Tag Recovery, Flux.charts readiness/detail lists, and Flux.serve Observed Health. Commands/browser checks: no live browser run; read notice context and mapped existing dashboard copy widgets in `web/Flux/src/templates/dashboard/home.html`. Findings: not reproduced yet. Blockers: needs running dashboard data to distinguish whole-table copy widgets from card/focus copy widgets. Remaining follow-up: verify presence/absence and top-right placement of table-level copy icons and identify where Flux.links/copy widgets can be reused.
- Outcome 2026-05-24 (Site Auditor cleanup audit): Representative surfaces checked with browser in seeded StaticLiveServer fixture: Ignition Bridges list, OPC server runtime list, Flux.live stale recovery list, Flux.charts dashboard links list, Flux.serve observed-health list, and real table on `/charts/?card=trace-samples&mode=detail`. Commands: `uv run python manage.py check`; `uv run pytest -q ../../site_audit/test_playwright_cleanup_audit.py`. Findings: real `<table>` copy button was inserted on chart samples, but it was not visible/click-ready in the fixture; dashboard table-like div lists had focus/card `.copy-corner` widgets but no table/list-level top-right copy buttons. Blockers: persistent dev-server data not checked. Remaining follow-up: make inserted real-table buttons visible/focusable and decide/implement list-level copy controls for table-like dashboard lists.

## Cleanup implementation note: 2026-05-24-dashboard-markup-follow-up

- Source: Cleanup
- Related notices: `2026-05-24-coordinator-006`, `2026-05-24-coordinator-007`, `2026-05-24-coordinator-009`, `2026-05-24-coordinator-011`
- Implemented cleanup to audit in browser: OPC runtime rows show stored state plus heartbeat evidence and PID/port when present; Flux.serve observed/heartbeat rows show PID/port metadata when present; `Default scope slug` is `Fluxolot`; the live import replace checkbox uses aligned checkbox-row markup; dashboard Flux.charts detail removes per-profile link lists and keeps aggregate chart-index plus `/charts/wells/`; chart CSV import has a `What is this?` disclosure with example layout; generic table copy buttons are inserted by `site.js` at table top-right.
- Still audit-owned: verify these are visible in a running dashboard after hard refresh/restart, verify table-copy placement across representative table surfaces, and keep pagination expectations open for large Flux.serve and Flux.charts lists.
- Outcome 2026-05-24 (Site Auditor cleanup audit): Browser fixture verified OPC PID/port evidence, Flux.serve PID/port/stale/unknown evidence, `Fluxolot` live default, aligned checkbox markup presence, dashboard Flux.charts aggregate links, chart CSV help, and charts path pagination. Remaining open after audit: Flux.serve long-list pagination/bounding and table/list copy visibility/coverage. Persistent hard-refresh dev-server check remains unrun.

## Cleanup implementation note: 2026-05-24-local-cleanup-pass

- Source: Cleanup
- Related notices: `2026-05-24-coordinator-002`, `2026-05-24-coordinator-007`, `2026-05-24-coordinator-009`, `2026-05-24-coordinator-011`
- Implemented cleanup to audit in browser: Ignition Bridges configure focus now has a `What is this?` disclosure explaining the WebDev Fluxy URL versus gateway admin URL, write-only token state, clear-token intent, version/build text, and docs link; Flux.live stale rows now include source context and isolate missing `Flux Field` provider rows as `legacy source missing` without deleting source data; Flux.charts `/charts/` path list is paginated with HTMX controls while preserving `/charts/wells/`; table copy affordance remains inserted by `site.js` for real `<table>` elements.
- Verification completed by Cleanup: focused dashboard/charts tests, full dashboard test module, TraceSmokeTests, `manage.py check`, skipped Playwright module under current environment, and strict MkDocs build.
- Still audit-owned: hard-refresh/browser-confirm the detail copy popover/docs link, table-copy button placement after HTMX swaps, and Flux.charts path pagination controls in a running dashboard with representative large data.

## Cleanup implementation note: 2026-05-24-table-pagination

- Source: Cleanup
- Related notices: `2026-05-24-coordinator-011`
- Implemented cleanup to audit in browser: table/list pagination is now server-rendered at 10 rows per page with HTMX Previous/Next swaps for Live tag table, Field endpoint/device/tag/node/heartbeat tables, Serve observed-health/heartbeat/log tables, Flux.charts path/sample lists, and Flux.cell bundle table. The previous client-side row-hiding paginator was removed; `site.js` now only inserts table copy controls for rendered table contents. Build HMI component selector remains unpaginated because it is a mutation form whose cross-page selection state needs a dedicated persistence design before rows can be hidden from the submitted DOM.
- Verification completed by Cleanup: focused server-pagination tests, affected Live/Field/Serve/Trace/DashboardStaticAsset suites, `manage.py check`, and relevant Playwright modules skipped because `FLUX_PLAYWRIGHT` is not enabled.
- Still audit-owned: browser-confirm 10-row pagination controls on representative Live, Serve, Field, Trace sample/path, and Cell tables after hard refresh and HTMX swaps; keep Build selector pagination as a product/architecture follow-up rather than a safe local cleanup.
- Follow-up implementation by Cleanup: dashboard Comp Surface list views for Flux.live stale recovery and Flux.serve observed health/heartbeats now also use the same 10-row server-side HTMX pagination via `live_stale_page` and `dashboard_serve_page`. Focused dashboard tests and full `dashboard.tests` passed; Playwright dashboard module skipped because `FLUX_PLAYWRIGHT` is not enabled.
- Follow-up implementation by Cleanup: missing `Flux Field` legacy rows are now split out of the Flux.live stale recovery queue. The main recovery list shows only active refreshable stale rows; legacy rows are summarized in one collapsed disclosure with a few full-path examples and are not submitted to the refresh action. Focused legacy-row tests and full `dashboard.tests` passed; Playwright dashboard module skipped because `FLUX_PLAYWRIGHT` is not enabled.
- Outcome 2026-05-24 (Site Auditor cleanup audit): Browser fixture confirmed detail copy popover/docs link, Flux.live source/legacy cleanup labels, chart path pagination controls, and chart CSV help. Remaining open: table-copy button placement/visibility after HTMX swaps is not clean because real table copy was inserted but hidden/not click-ready, and table-like dashboard lists still lacked list-level copy buttons. Persistent hard-refresh dev-server check remains unrun.
