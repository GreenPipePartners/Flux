# Web Applications

The `web/` directory contains Django/HTMX web application workspaces.

## Sections

- `Flux/`: clean Flux application. This is the active Ignition companion server direction.

Legacy Drew migration workbench code was removed. Flux owns the active Django/HTMX direction.
