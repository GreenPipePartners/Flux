# Security Daily Log — 2026-05-24

## Session — daily cybersecurity review

- **Timestamp:** not captured; local date 2026-05-24.
- **Task intent:** Perform dependency-scoped daily cybersecurity review for Flux and update the Threat Watch reports/logs.
- **Files inspected:** Python manifests/locks for `web/Flux`, `fluxy`, `build`, `mine`, `sim`, `deep`; `.opencode/package.json`; `.opencode/package-lock.json`; `web/Flux/src/flux/settings.py`; `web/Flux/src/flux/urls.py`; `web/Flux/src/flux/asgi.py`; `web/Flux/src/templates/flux/base.html`; `web/Flux/src/static/flux/vendor/README.md`; `docs/operator-guide.md`; `security/threat_watch.md`; `security/dependency_exposure.md`; `security/source_notes.md`; `security/environment_variables.md`.
- **Repository searches:** Django auth/admin/security middleware, mutating POST views, QuestDB DSNs/default credentials, public CDN usage, Fluxy WebDev auth/token/resource handlers, Ignition/PostgreSQL/QuestDB references.
- **Commands run:**
  - `git status --short`
  - `git diff -- security/threat_watch.md security/dependency_exposure.md security/source_notes.md security/core_area_files.md`
  - `uv tree --locked --all-groups --depth 2` in `web/Flux`, `fluxy`, `build`, `mine`, `sim`, and `deep`
  - `uv pip list` in `web/Flux`
  - Two unsupported Fluxy `uv tree` option attempts failed before using the supported command.
- **Sources queried:** CISA KEV catalog; CISA ICS advisory index for Inductive Automation; CISA ICSA-26-071-06 and ICSA-25-352-01; Django dev and 5.2 security archives; PostgreSQL security page, CVE-2026-6479 detail, and 2026-05-14 release announcement; GitHub Security Advisories for Django, httpx, psycopg, Starlette, python-multipart, OpenCode, and cross-spawn; OSV list pages for Django/gunicorn/waitress/starlette/playwright; CVE Program pages for selected CVEs; QuestDB release-note URL attempts.
- **Findings:**
  - No direct CISA KEV match promoted; Laravel Ignition in KEV is not Inductive Automation Ignition.
  - PostgreSQL deployed server version is the top version-evidence blocker after the 2026-05-14 PostgreSQL security release for supported 14-18 versions.
  - Ignition Gateway version/platform/service-account posture is a blocker for recent CISA ICS advisories CVE-2025-13913 and CVE-2025-13911.
  - Django is locked at 5.2.14 and appears current relative to retrieved Django security release stream.
  - Optional Fluxy MCP `starlette==1.0.0` and `python-multipart==0.0.28` are above checked vulnerable ranges; only exposed if `fluxy-mcp` runs.
  - `cross-spawn==7.0.6` is above the vulnerable `>=7.0.0,<7.0.5` range for CVE-2024-21538.
  - Mutating Flux routes still lack observed auth decorators, and Django dev defaults remain local-only risks.
- **Blockers/limitations:** OSV batch matching, `pip-audit`, npm audit, and direct psycopg/libpq runtime inspection were blocked by local command policy; CVE Program pages rendered JavaScript-only placeholders; QuestDB release-note URLs returned 404; deployed PostgreSQL, QuestDB, Ignition, Python, Playwright browser, OS package, and env-var values were unavailable.
- **Files updated:** `security/threat_watch.md`, `security/dependency_exposure.md`, `security/source_notes.md`, `security/core_area_files.md`, and this daily log.
- **Next actions:** Capture deployed PostgreSQL/Ignition/QuestDB/Python/browser versions; patch PostgreSQL to the fixed 2026-05-14 minor release for its major line; verify Ignition 8.3.0+ or document hardening; define `Flux.auth` before non-local exposure; run OSV Scanner/`pip-audit`/npm audit when allowed.

## Session — permission follow-up and blocked rerun

- **Timestamp:** not captured; local date 2026-05-24.
- **Task intent:** Proceed with deeper Threat-Watch runtime/version and dependency audit after granting the agent more defensive Bash permissions.
- **Files inspected:** `.opencode/agents/threat-watch.md`; `security/threat_watch.md`; `security/dependency_exposure.md`; `security/source_notes.md`; `security/core_area_files.md`; this daily log.
- **Commands attempted:** `git status --short`; `git diff -- security/... .opencode/agents/threat-watch.md`; attempted but blocked by the still-active old Bash allowlist: `uv --version`, `python --version`, `node --version`, `npm --version`, `uv run python --version`, `uv run python -m django --version`, `uv run python -m playwright --version`, `psql --version`, and `curl -fsS http://127.0.0.1:9000/exec?query=select%20version%28%29`.
- **Findings:** No new vulnerability evidence collected. The `.opencode/agents/threat-watch.md` file now contains the expanded defensive allowlist, but OpenCode did not hot-reload it into this running session.
- **Blockers/limitations:** Deeper audit/version commands remain blocked until OpenCode is restarted. No OSV Scanner, `pip-audit`, npm audit, PostgreSQL/QuestDB runtime version, Python patch, or Playwright version evidence was collected in this session.
- **Files updated:** `security/threat_watch.md`, `security/source_notes.md`, `security/core_area_files.md`, and this daily log.
- **Next actions:** Quit/restart OpenCode, then rerun Threat-Watch so the updated allowlist is active; run version probes and package audits before changing exposure statuses.

## Session — post-restart deeper dependency audit

- **Timestamp:** not captured; local date 2026-05-24.
- **Task intent:** Retry the previously blocked Threat-Watch runtime/version and dependency audit after the user closed and restarted OpenCode.
- **Files inspected:** Python manifests and locks for `web/Flux`, `fluxy`, `build`, `mine`, `sim`, and `deep`; `.opencode/package.json`; `.opencode/package-lock.json`; `fluxy/src/fluxy/mcp/server.py`; current security reports/logs.
- **Repository searches:** `idna` and `starlette` lock evidence; Starlette/MCP usage; Starlette `request.url`/path usage in Fluxy; runtime version references; dependency manifests.
- **Commands run:** `git status --short`; `uv --version`; `python --version`; `node --version && npm --version`; `psql --version`; `uv run python --version`; `uv run python -c "import django; print(django.get_version())"`; `uv run python -m playwright --version`; psycopg/libpq version probe via `uv run python -c`; localhost QuestDB `/exec?query=select version()` probe; `uv tree --locked --all-groups --depth 2`; full Fluxy `uv tree --locked --all-groups`; `uv pip list`; `uv pip list --format=freeze`; `uv tool run pip-audit --version`; `uvx pip-audit --version`; `uv tool run pip-audit` project audits; temporary pinned requirement file audits with `uv tool run pip-audit --no-deps --requirement`; direct `pip-audit --version`; direct `osv-scanner --version`; `uvx osv-scanner --version`; `npm audit --audit-level=low`.
- **Commands still blocked or unsuccessful:** direct `uv run python -m django --version` still blocked by allowlist; `curl` with a quoted localhost URL was blocked but the unquoted allowed form succeeded; `uv run python -m pip list --format=freeze` failed because project virtualenvs do not include pip; `uv tool run pip-audit --locked .` found no recognized lockfiles; `uv tool run pip-audit` project audits failed for `web/Flux` and `build` because local editable packages were treated as PyPI package names; `uv export` process substitution audit commands were blocked; `uvx osv-scanner` failed because OSV Scanner is not a Python package.
- **Sources queried:** CISA KEV feed; GitHub/OSV/upstream advisory pages for idna GHSA-65pc-fj4g-8rjx / CVE-2026-45409; Starlette GitHub advisory GHSA-86qp-5c8j-p5mr / CVE-2026-48710; OSV PYSEC-2026-161; X41 Starlette advisory; PyPI idna and Starlette release pages; PostgreSQL security page, CVE-2026-6477 detail, and 2026-05-14 release announcement.
- **Findings:**
  - `idna==3.14` is confirmed affected in web/Flux and Fluxy locks by CVE-2026-45409 / GHSA-65pc-fj4g-8rjx; fixed in 3.15, with 3.16 available on PyPI.
  - Optional Fluxy MCP `starlette==1.0.0` is confirmed affected by PYSEC-2026-161 / GHSA-86qp-5c8j-p5mr / CVE-2026-48710; fixed in 1.0.1, with 1.1.0 available on PyPI.
  - Local psycopg-binary reports bundled libpq 18.0 and local `psql` is 18.3, below PostgreSQL's 18.4 fixed minor for May 2026 client issues including CVE-2026-6477.
  - Django runtime is confirmed 5.2.14; Playwright is confirmed 1.59.0; local Python is 3.14.4; uv is 0.11.11.
  - Local QuestDB endpoint responded, but only with PostgreSQL compatibility string `PostgreSQL 12.3, compiled by Visual C++ build 1914, 64-bit, QuestDB`; authoritative QuestDB product version remains unresolved.
  - `.opencode` npm audit found 0 vulnerabilities.
- **Files updated:** `security/threat_watch.md`, `security/dependency_exposure.md`, `security/source_notes.md`, `security/core_area_files.md`, and this daily log. Temporary requirement files under `security/` were created for audit and deleted before the session ended.
- **Next actions:** Upgrade/re-lock `idna>=3.15` and optional MCP `starlette>=1.0.1`; update PostgreSQL client/libpq/psql to 18.4+ or an equivalent fixed minor; capture deployed PostgreSQL server, Ignition, and authoritative QuestDB versions; add Flux auth gates before non-local exposure; run OSV Scanner from a valid packaged binary or approved npm invocation.

## Session — dependency remediation

- **Timestamp:** not captured; local date 2026-05-24.
- **Task intent:** Patch the confirmed Python dependency findings from the post-restart Threat-Watch audit.
- **Files changed:** `web/Flux/pyproject.toml`, `web/Flux/uv.lock`, `fluxy/pyproject.toml`, `fluxy/uv.lock`, `security/threat_watch.md`, `security/dependency_exposure.md`, `security/source_notes.md`, and this daily log.
- **Commands run:** `uv lock --upgrade-package idna` in `web/Flux`; `uv lock --upgrade-package idna --upgrade-package starlette` in `fluxy`; `uv lock` in both projects after adding explicit manifest floors; `uv sync --all-groups` in `web/Flux`; `uv sync --all-groups --all-extras` in `fluxy`; `uv tree --locked --all-groups --depth 2` in both projects; patched runtime import checks for `idna` and `starlette`; `uv tool run pip-audit --skip-editable --progress-spinner off --path ...site-packages` for both synced environments; `uv lock --upgrade-package psycopg --upgrade-package psycopg-binary`; psycopg/libpq runtime version check.
- **Findings/remediation:**
  - Web and Fluxy now explicitly require `idna>=3.15`; both locks resolve `idna==3.16`; synced runtime checks returned `3.16`.
  - Fluxy MCP extra now explicitly requires `starlette>=1.0.1`; Fluxy lock resolves `starlette==1.1.0`; synced runtime check returned `1.1.0`.
  - `pip-audit` against the synced web and Fluxy site-packages reported no known vulnerabilities. Local editable packages were skipped as expected.
  - `uv lock --upgrade-package psycopg --upgrade-package psycopg-binary` found no newer psycopg package in the current resolver state; psycopg remains 3.3.4 and bundled libpq remains 18.0. Local `psql` remains an OS/client package boundary at 18.3.
- **Remaining next actions:** Deploy/sync the updated locks in any runtime environment; update PostgreSQL client/libpq/psql to 18.4+ outside this repo when available; capture deployed PostgreSQL server, Ignition, and authoritative QuestDB versions; continue planning Flux auth before non-local exposure.
